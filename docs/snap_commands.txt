# Run these commands from within the electron/ directory
npx electron-packager . earthcal --platform=linux --arch=x64 --out=./ --overwrite --prune=true
snapcraft pack
snapcraft
sudo snap install --dangerous earthcal_*.snap

snapcraft login
snapcraft upload earthcal_1.1.5_amd64.snap

snapcraft status earthcal
sudo snap refresh earthcal --edge
